/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig
module.exports = {
    eslint: {
      // Ignore les erreurs ESLint lors des étapes de build
      ignoreDuringBuilds: true,
    },
  }